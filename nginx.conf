# HTTP → HTTPS Redirect
server {
    listen 80;
    listen [::]:80;
    server_name tinyrisks.art www.tinyrisks.art;
    return 301 https://$host$request_uri;
}

# HTTPS Server
server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name tinyrisks.art www.tinyrisks.art;

    # SSL Certificate (Let's Encrypt)
    ssl_certificate /etc/letsencrypt/live/tinyrisks.art/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/tinyrisks.art/privkey.pem;

    # Document root
    root /var/www/tinyrisks.art/htdocs;

    # Maximum upload size (matching Flask's 20MB limit)
    client_max_body_size 20M;

    # Serve static files directly
    location /static/ {
        alias /var/www/tinyrisks.art/htdocs/static/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Proxy all other requests to Flask application
    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # HTTP version for better compatibility
        proxy_http_version 1.1;
        
        # Timeouts (increased to 120s for large file uploads up to 20MB)
        proxy_connect_timeout 60s;
        proxy_send_timeout 120s;
        proxy_read_timeout 120s;
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
}
